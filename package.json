{
    "name": "event-management",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "build": "npm install --prefix client && npm install --prefix server && npm --prefix client run build && cp -r client/build server/public",
      "start": "npm --prefix server run start"
    }
  }
